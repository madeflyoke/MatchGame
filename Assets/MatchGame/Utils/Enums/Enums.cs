public enum CategoryType
{
    Food,
    Vehicle,
}

public enum PlayerState
{
    None,
    MovingLeft,
    MovingRight,
}

public enum Layer
{
    CardCorrectAnswer = 10,
    CardWrongAnswer = 11
}
